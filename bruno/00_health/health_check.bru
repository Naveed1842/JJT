meta:
  name: health_check
  type: http

request:
  url: {{baseUrl}}/actuator/health
  method: GET
  headers:
    X-ROLE: {{orgRole}}

tests:
  - name: status-is-200
    script: |
      expect(response.status).toBe(200);
      expect(response.json().status).toBeDefined();
